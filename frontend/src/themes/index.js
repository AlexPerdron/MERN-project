import { createTheme } from "@material-ui/core";

export const themes = createTheme({
    palette: {
        primary: {
            main: '#f50202',
        },
        secondary: {
            main: '#3f51b5',
        },
        error: {
            main: '#f3e5f5',
        }
    },
});